## Usage
- Download or clone this repo to access files.
- Open `code class_EDE.docx` and `results.docx` with Microsoft Word or a compatible viewer.
- Note: These are placeholders—real code (e.g., `class_EDE.py`) and data (e.g., `ede_simulation_results.txt`) will follow.
